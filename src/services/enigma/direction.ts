/**
 * Signal direction through rotor wiring.
 * @enum {string}
 */
export enum Direction {
  /** Signal flowing forward (left to right through rotors) */
  FORWARD = 'FORWARD',
  /** Signal flowing backward (right to left through rotors) */
  BACKWARD = 'BACKWARD',
}
